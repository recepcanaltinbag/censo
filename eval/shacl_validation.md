# SHACL validation of the knowledge graph

Generated by `scripts/18_shacl_validate.py`.

- graph: `censo-waterbase.ttl` plus the ontology, **651,373 triples**
- conforms: **False** (6085.7\,s)
- distinct violation types: **1**

**What the shapes could see.** A validation that reports no violations over a graph in which the shapes match nothing is not a result, and this run reported exactly that until subclass type assertions were materialised below: the ABox types observations by their detection status, SHACL does not reason, and a shape targeting `censo:AssessedObservation` sees a `censo:CensoredObservation` only if something has drawn the connection. Until the OWL 2 RL repair that connection was a union definition, which no subclass axiom expressed and which this script had to special-case by hand; it is an ordinary `rdfs:subClassOf` now, so one closure covers it.

| shape target | nodes |
|---|---|
| `ComplianceOutcome` | 40,000 |
| `AssessedObservation` | 40,000 |
| `CensoredObservation` | 31,167 |
| `FeatureOfInterest` | 8,510 |
| `DetectedObservation` | 4,151 |
| `AnalyticalMethod` | 1,159 |
| `Threshold` | 537 |
| `Campaign` | 48 |
| `GroupThreshold` | 4 |
| `RegulationPackage` | 2 |

| n | message |
|---|---|
| 4646 | An observation must cite the analytical method used; without it, no detection limit applies and the record is unresolvable. |

Each is a defect in the source data or in this pipeline, which is what the shapes exist to surface.
