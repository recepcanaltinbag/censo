# OWL 2 RL profile check

Generated by `scripts/12_owl_profile.py`. The manuscript claims the vocabulary stays inside OWL 2 RL; this checks the claim against the grammar in OWL 2 Profiles §4.3 rather than against a reasoner. A reasoner cannot detect a profile violation, because applying more rules than the profile requires is precisely what hides one --- which is how these went unnoticed while the pipeline ran correctly.

- axioms outside the profile: **0**

The vocabulary is inside the profile.

