# What the other vocabularies cannot separate

Generated by `scripts/13_separation.py`. The gap table records which vocabulary declares which term; this asks the question a term list cannot answer — whether the readings a record admits stay different once written in that vocabulary.

## The witness

One row of the EEA record, the same one Figure 6(b) is drawn from: **Imidacloprid** (CAS 138261-41-3) at a river station in ES, reported below a quantification limit of **0.01 µg/L**, carrying **0.01** as its value, against an annual-average standard of **0.0068 µg/L**. The standard lies inside the censored interval and the reported value is the limit itself, which is what substitution at source looks like in this release.

Three readings of those same four numbers:

| reading | what it says | facts asserted |
|---|---|---|
| **R1** | censoring honoured | `identity`, `value`, `limit`, `limit_here`, `censored`, `interval`, `threshold`, `no_verdict` |
| **R2** | number taken at face value | `identity`, `value`, `limit`, `limit_here`, `threshold` |
| **R3** | neither flag nor limit reported | `identity`, `value`, `threshold` |

`R1` and `R2` are the pair the paper turns on: identical numbers, different claims.

## The facts, and the concept family each needs a term for

| fact | statement | family required |
|---|---|---|
| `identity` | the observation exists, with its analyte, station and year | `observation` |
| `value` | the number 0.01 ug/L is reported for it | `result` |
| `limit` | a quantification limit of 0.01 ug/L is stated | `detection_limit` |
| `limit_here` | that limit qualifies THIS result, not the device that produced it | `detection_limit_on_result` |
| `censored` | the number is a bound, not a measurement | `censoring` |
| `interval` | the result is the interval [0, 0.01], not the point 0.01 | `censoring+interval_result` |
| `threshold` | an annual-average standard of 0.0068 ug/L applies | `threshold` |
| `no_verdict` | the verdict is that no verdict is supportable | `undecidable` |

`observation` and `result` are granted to every vocabulary without checking, since all of them are observation vocabularies. Every other family is decided by `scripts/07_verify_gap_table.py` parsing the ontology file. The translation is generous by construction: a fact survives if the vocabulary has **any** term in its family.

## Translations

| vocabulary | τ(R1) | τ(R2) | τ(R3) | R1 vs R2 |
|---|---|---|---|---|
| SOSA/SSN | `identity`, `value` | `identity`, `value` | `identity`, `value` | **collapse** |
| SOSA core | `identity`, `value` | `identity`, `value` | `identity`, `value` | **collapse** |
| GeoSPARQL | `identity`, `value` | `identity`, `value` | `identity`, `value` | **collapse** |
| QUDT schema | `identity`, `threshold`, `value` | `identity`, `threshold`, `value` | `identity`, `threshold`, `value` | **collapse** |
| SAREF core | `identity`, `threshold`, `value` | `identity`, `threshold`, `value` | `identity`, `threshold`, `value` | **collapse** |
| SAREF4WATR v2.1.1 | `identity`, `threshold`, `value` | `identity`, `threshold`, `value` | `identity`, `threshold`, `value` | **collapse** |
| SSN-system | `identity`, `limit`, `value` | `identity`, `limit`, `value` | `identity`, `value` | **collapse** |
| OWL-Time | `identity`, `value` | `identity`, `value` | `identity`, `value` | **collapse** |
| WHOW water-monitoring | `identity`, `value` | `identity`, `value` | `identity`, `value` | **collapse** |
| DoCE (Rio Doce WQ) | `identity`, `value` | `identity`, `value` | `identity`, `value` | **collapse** |
| InWaterSense core | `identity`, `threshold`, `value` | `identity`, `threshold`, `value` | `identity`, `threshold`, `value` | **collapse** |
| InWaterSense regulations | `identity`, `value` | `identity`, `value` | `identity`, `value` | **collapse** |
| InWaterSense pollutants | `identity`, `value` | `identity`, `value` | `identity`, `value` | **collapse** |
| WAM-ONTO | `identity`, `value` | `identity`, `value` | `identity`, `value` | **collapse** |
| SewerNet | `identity`, `value` | `identity`, `value` | `identity`, `value` | **collapse** |
| CHMO (chemical methods) | `identity`, `limit`, `no_verdict`, `value` | `identity`, `limit`, `value` | `identity`, `value` | separated |
| AFO (Allotrope) | `identity`, `threshold`, `value` | `identity`, `threshold`, `value` | `identity`, `threshold`, `value` | **collapse** |
| STATO (statistics) | `identity`, `threshold`, `value` | `identity`, `threshold`, `value` | `identity`, `threshold`, `value` | **collapse** |
| ENVO | `identity`, `value` | `identity`, `value` | `identity`, `value` | **collapse** |
| ExO (exposure) | `identity`, `value` | `identity`, `value` | `identity`, `value` | **collapse** |
| Project ontology 2018 | `identity`, `limit`, `value` | `identity`, `limit`, `value` | `identity`, `value` | **collapse** |

## Result

- vocabularies assessed: **21**
- R1 and R2 translate to the same facts in: **20 of 21**
- R1 and R3 translate to the same facts in: **18 of 21**
- R2 and R3 translate to the same facts in: **18 of 21**

**1 of 21 vocabularies separate R1 from R2**: CHMO (chemical methods).

- **CHMO (chemical methods)** — credited with the `undecidable` family on two terms that are not assessment outcomes: `ambiguous synonym` (OMO_0003001), an OBO annotation property describing the quality of a SYNONYM, and `double quantum transitions for finding unresolved lines` (CHMO_0001861), an NMR technique in which the unresolved things are spectral lines. R1 therefore gains `no_verdict` and R2 does not. CHMO declares no censoring term at all, so it cannot separate a bound from a measurement -- which is the distinction that matters -- and read strictly it collapses with the rest.

A separation listed above as a documented exception is an artefact of the matcher, not a capability: the concept patterns are deliberately generous, so a vocabulary can be credited with a family on a term that means something else, and the fact it then gains in one reading is enough to break the collapse. The cells are left standing and the discrepancy published, because narrowing a pattern until a competitor loses one would make the comparison an assertion again.

R1 and R3 are separated by SSN-system, CHMO (chemical methods), Project ontology 2018 — but only by the presence or absence of a limit somewhere in the record, not by what the number means. A vocabulary can notice that a limit was stated; none can say that this result fell below it.

### Sensitivity: granting the interval without the censoring term

`interval` is written above as needing both a censoring term and an interval-valued result, because without the first there is no ground on which to assert that the result is an interval. Granting it on the interval family alone — the most favourable reading available to a vocabulary with interval results and no censoring class — separates R1 from R2 in: **WHOW water-monitoring, InWaterSense core, CHMO (chemical methods), AFO (Allotrope), STATO (statistics)**. Even there the interval must be asserted by hand from a flag the vocabulary has no term for, which is the distinction at issue rather than a counter-example to it.

## The same three readings in CENSO

| reading | detection status entailed | compliance outcome entailed |
|---|---|---|
| **R1** (censoring honoured) | `censo:CensoredObservation` | `censo:IndeterminateCompliance`, `censo:MethodInsufficient` |
| **R2** (number taken at face value) | `censo:QuantifiedObservation` | `censo:Exceedance` |
| **R3** (neither flag nor limit reported) | `censo:UnresolvedObservation` | — |

Outcome classes are entailed, not asserted: `censo:Exceedance` is an equivalent class of an observation bearing `censo:exceeds` to a threshold, so R2's verdict follows from the comparison the rule layer materialises. R1 carries no comparison property at all — Article 3(3b) sets the result aside, which is a regulatory verdict and not a comparison.

### The readings are incompatible, not merely differently labelled

A vocabulary that only wrote a different word for each reading would leave a graph free to hold both at once. Merging any two readings of the same individual must therefore be an inconsistency.

| merged | inconsistent | what the reasoner reports |
|---|---|---|
| R1 + R2 | **yes** | Disjoint classes https://w3id.org/censo/CensoredObservation and https://w3id.org/censo/QuantifiedObservation h… |
| R1 + R3 | **yes** | Disjoint classes https://w3id.org/censo/CensoredObservation and https://w3id.org/censo/UnresolvedObservation h… |
| R2 + R3 | **yes** | Disjoint classes https://w3id.org/censo/QuantifiedObservation and https://w3id.org/censo/UnresolvedObservation… |

All three merges are contradictions, from the `owl:AllDisjointClasses` axiom over the detection statuses. The separation is therefore a property of the vocabulary and not of the pipeline that populates it: a loader cannot quietly hold two readings of one record.

## What this does not claim

Not that these vocabularies are wrong, and not that they could not be extended — any of them could be given a censoring class tomorrow. The claim is about reuse as published, which is what a comparison table is for, and about where the distinction has to live once it is added. A limit declared as a property of the sensor, as SSN-system declares it, describes what an instrument can do; separating these readings needs a limit bound to the result and a status on the observation.

